#include<stdio.h>

int fact(int x)
{
     int n;
    scanf("%d", &n);

    int a=1;
    for(int i=1; i<=n; i++)
    {
        a=a*i;
    }
    printf("%d", a);
}
int main()
{
    int fact(int);

    int n;

    fact(n);
return 0;
}
